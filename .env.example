# Leave these unset to run fully local (localStorage only, single device).
# Set both to turn on Supabase sync and passcode sign-in. See docs/SUPABASE_SETUP.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
